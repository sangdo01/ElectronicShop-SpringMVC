package ElectronicShop.Cotroller.Admin;

import javax.servlet.http.HttpSession;

import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.ModelAndView;

import ElectronicShop.Dao.BaseDao;

@Controller
public class ProductManageController extends BaseAdminController {

	/*
	 * @RequestMapping(value = { "/admin/list-product" }) public ModelAndView
	 * listProduct(HttpSession session) {
	 * 
	 * if (session.getAttribute("InfoAdmin") != null) {
	 * 
	 * } else { _mvShare.setViewName("redirect:/"); }
	 * 
	 * _mvShare.addObject("listSlides", slideService.getAllSlides());
	 * _mvShare.setViewName("admin/slide/list_slide"); return _mvShare; }
	 */

}
